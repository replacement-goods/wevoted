import React from 'react';
import './styles.scss';
import InnerContainer from '../../elements/InnerContainer';

const Problem = () => {
  return (
    <div className="problemContainer">
      <InnerContainer>The Problem</InnerContainer>
    </div>
  );
};

export default Problem;