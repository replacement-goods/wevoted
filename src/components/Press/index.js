import React from 'react';
import './styles.scss';
import InnerContainer from '../../elements/InnerContainer';

const Press = () => {
  return (
    <div className="pressContainer">
      <InnerContainer>Press section</InnerContainer>
    </div>
  );
};

export default Press;